type PrismaClientLike = { tenant: { upsert: (input: unknown) => Promise<{ id: string }> }; user: { upsert: (input: unknown) => Promise<unknown> }; workflow: { upsert: (input: unknown) => Promise<unknown> }; $disconnect: () => Promise<void> };
async function main() { const prismaModule = await import("@prisma/client"); const PrismaClient = (prismaModule as unknown as { PrismaClient?: new () => PrismaClientLike; default?: { PrismaClient?: new () => PrismaClientLike } }).PrismaClient ?? (prismaModule as unknown as { default?: { PrismaClient?: new () => PrismaClientLike } }).default?.PrismaClient; if (!PrismaClient) throw new Error("Prisma client is not generated"); const prisma = new PrismaClient(); const tenant = await prisma.tenant.upsert({ where: { slug: "demo" }, update: {}, create: { name: "Nexus Demo Workspace", slug: "demo", region: "eu-central" } }); await prisma.user.upsert({ where: { tenantId_email: { tenantId: tenant.id, email: "owner@nexus.local" } }, update: {}, create: { tenantId: tenant.id, email: "owner@nexus.local", name: "Workspace Owner", role: "OWNER" } }); await prisma.workflow.upsert({ where: { id: "demo-workflow" }, update: {}, create: { id: "demo-workflow", tenantId: tenant.id, name: "Purchase order review", status: "ACTIVE", definition: { trigger: "purchase_order.created", steps: ["extract", "validate", "approve"] } } }); await prisma.$disconnect(); }
void main();
