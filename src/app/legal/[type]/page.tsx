import { notFound } from "next/navigation";
import PlatformPage from "@/components/shared/PlatformPage";
const data = { privacy: { eyebrow: "Legal", title: "Privacy that respects", highlight: "the operation.", description: "Nexus AI is built around clear data boundaries, responsible processing, and customer control." }, terms: { eyebrow: "Legal", title: "Terms for a trusted", highlight: "partnership.", description: "The principles governing access to Nexus AI services and platform capabilities." }, cookies: { eyebrow: "Legal", title: "A clearer view of", highlight: "cookies.", description: "How Nexus uses cookies and similar technologies to keep the experience secure and useful." } };
export function generateStaticParams() { return Object.keys(data).map(type => ({ type })); }
export default async function LegalPage({ params }: { params: Promise<{ type: string }> }) { const { type } = await params; const page = data[type as keyof typeof data]; if (!page) notFound(); return <PlatformPage {...page} primaryCta="Contact legal" primaryHref="/contact" secondaryCta="Back to home" secondaryHref="/" sections={[{ title: "Clear commitments", description: "We make our operating principles legible to the people trusting us with important work.", bullets: ["Purpose-limited processing", "Access controls", "Transparent subprocessors"] }, { title: "Questions welcome", description: "For specific questions about your organization, contact the Nexus team.", bullets: ["Data processing enquiries", "Security questions", "Enterprise agreements"] }]} />; }
