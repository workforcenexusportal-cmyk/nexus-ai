"use client";
import { FormEvent, useState } from "react";
import { signIn } from "next-auth/react";
import { Loader2, LockKeyhole, Sparkles } from "lucide-react";
export default function LoginPage() { const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); const form = new FormData(event.currentTarget); const result = await signIn("credentials", { email: form.get("email"), tenantId: form.get("tenantId"), redirect: true, callbackUrl: "/client-portal" }); if (result?.error) { setError("Workspace not found. Use the workspace ID created for your account."); setLoading(false); } } return <main className="dark flex min-h-[75vh] items-center justify-center bg-nexus-bg px-4 py-24 text-nexus-text"><div className="w-full max-w-md rounded-3xl border border-nexus-border/60 bg-nexus-card/80 p-8 shadow-2xl"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-gradient-to-br from-nexus-accent to-nexus-accent2"><Sparkles className="h-5 w-5 text-nexus-bg" /></div><h1 className="mt-7 text-3xl font-bold">Open your control plane.</h1><p className="mt-3 text-sm leading-relaxed text-nexus-secondary">Sign in to manage workflows, agents, data, and workspace policies.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm text-nexus-secondary">Email<input name="email" type="email" required placeholder="you@company.com" className="mt-2 w-full rounded-xl border border-nexus-border bg-nexus-bg px-4 py-3 text-white outline-none focus:border-nexus-accent" /></label><label className="block text-sm text-nexus-secondary">Workspace ID<input name="tenantId" required placeholder="your-workspace-id" className="mt-2 w-full rounded-xl border border-nexus-border bg-nexus-bg px-4 py-3 text-white outline-none focus:border-nexus-accent" /></label>{error && <p className="text-sm text-red-300">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-nexus-accent to-nexus-accent2 px-5 py-3 font-semibold text-nexus-bg disabled:opacity-60">{loading ? <Loader2 className="h-4 w-4 animate-spin" /> : <LockKeyhole className="h-4 w-4" />} {loading ? "Opening..." : "Sign in"}</button></form><p className="mt-5 text-xs text-nexus-secondary">Demo workspace: <code className="text-nexus-accent">demo</code> · owner@nexus.local</p></div></main>; }
