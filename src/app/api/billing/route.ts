import { getTenantContext, requireRole } from "@/lib/backend";
export const dynamic = "force-dynamic";
const plans = [{ id: "pilot", name: "Transformation Sprint", price: "€2,500–€10,000", interval: "fixed", features: ["Defined workflow scope", "ROI model", "Production path"] }, { id: "growth", name: "Growth", price: "€2,999", interval: "month", features: ["10 AI agents", "10,000 tasks", "Priority support"] }, { id: "scale", name: "Scale", price: "€7,999", interval: "month", features: ["Unlimited workflows", "Advanced analytics", "Dedicated success"] }, { id: "enterprise", name: "Enterprise", price: "Custom", interval: "contract", features: ["SSO and SCIM", "Data residency", "SLA and dedicated support"] }];
export async function GET(request: Request) { return Response.json({ data: { tenantId: getTenantContext(request).tenantId, plan: "pilot", status: "trial", plans } }); }
export async function POST(request: Request) { try { const context = getTenantContext(request); requireRole(context, ["owner", "admin"]); const body = await request.json() as { plan?: string }; const plan = plans.find(item => item.id === body.plan); if (!plan) return Response.json({ error: "PLAN_NOT_FOUND" }, { status: 400 }); return Response.json({ data: { checkoutRequired: true, plan: plan.id, message: "Connect Stripe Checkout in the deployment environment to create the live subscription." } }, { status: 202 }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "FORBIDDEN" }, { status: 403 }); } }
